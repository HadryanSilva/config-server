server:
  port: 8081
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=product_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop